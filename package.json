{
  "name": "programmatic-seo-site",
  "version": "1.0.0",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve",
    "generate": "node scripts/generate.js",
    "deploy": "npx gh-pages -d _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "csv-parse": "^5.5.5",
    "dotenv": "^16.4.5",
    "gh-pages": "^6.1.1",
    "luxon": "^3.4.4"
  }
}